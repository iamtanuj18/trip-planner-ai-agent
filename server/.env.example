# Copy this to .env and fill in your values.

# Bedrock uses us-east-1 for Nova Lite cross-region inference (the us. model prefix).
# Do not change this unless you are using a different model without a region prefix.
AWS_REGION=us-east-1

# AWS credentials for local development.
# If ~/.aws/credentials is already configured you can leave these blank.
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Amazon Nova Lite via Bedrock — no access approval needed, supports tool use
BEDROCK_MODEL_ID=us.amazon.nova-lite-v1:0

# Rate limits — reset at midnight and the 1st of the month (Melbourne time)
MAX_DAILY_REQUESTS=50
MAX_MONTHLY_REQUESTS=500

# Comma-separated origins allowed by CORS.
# Add your Amplify URL when deploying.
ALLOWED_ORIGINS=http://localhost:5173

# DynamoDB session storage.
# DYNAMODB_REGION is separate from AWS_REGION because Bedrock needs us-east-1
# while the DynamoDB table lives in ap-southeast-2.
SESSIONS_TABLE=trip-planner-sessions
DYNAMODB_REGION=ap-southeast-2

# LangSmith observability (optional).
# Sign up at https://smith.langchain.com to get a free API key.
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=your-langsmith-api-key
LANGSMITH_PROJECT=trip-planner-ai

# Production only — set this to the Secrets Manager secret name that holds
# LANGSMITH_API_KEY so it never has to appear in apprunner.yaml.
# Leave blank for local development (LANGSMITH_API_KEY above is used instead).
LANGSMITH_SECRET_NAME=
